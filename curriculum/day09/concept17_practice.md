# 🛠️ Concept 17 Practice: Breaking Out

### 🚀 The 20-Problem Challenge Menu
*Note: Using `while True` and `break` is how real pros build games!*

#### 🟢 Group A: The Emergency Stop (Warmups)
1.  **Stop at 5:** Write a loop from 1 to 10, but `break` if the number is 5.
2.  **The Quitter:** Create a `while True` loop that asks for a word. `break` if the word is "quit".
3.  **Found Item:** Loop through a list of items. If you see "Gold", print "FOUND" and `break`.
4.  **Count Break:** Use a counter starting at 0. `while True`, add 1 and `break` when it hits 10.
5.  **The Secret Word:** `break` a loop if the user enters the secret word "Pineapple".

#### 🟡 Group B: Real World Escapes
6.  **The Health Guard:** A loop that simulates a fight. If `hp` hits 0, `break` and print "YOU DIED".
7.  **The Searcher:** Search a list of 10 names. `break` as soon as you find "Alex" to save time.
8.  **The ATM:** A loop that asks for a PIN. If correct, `break` and say "Access Granted".
9.  **Battery Drain:** A loop that subtracts 10% battery. If it reaches 0, `break` and say "System Shutdown".
10. **The Food Order:** Ask the user "What do you want to eat?" until they say "I'm full".

#### 🔴 Group C: The Bug Squasher (Fix the Break)
11. **The Floating Break:** `break` on a line with no loop (Fix the syntax error!).
12. **The Always Break:** An `if True: break` at the very top of a loop (Fix it so the loop actually runs once!).
13. **Missing Logic:** A `while True` loop with no `break` at all (Fix the infinite loop!).
14. **The Indent Issue:** `break` is not pushed to the right enough.
15. **The Wrong Word:** Trying to use `exit` or `stop` instead of `break`.

#### 💎 Group D: The AI Whisperer (Gemini Missions)
16. **The Game Loop:** Ask Gemini: "Write a `while True` game loop that tracks a score and breaks if the score reaches 100."
17. **The Match Maker:** Ask Gemini: "How do I use `break` to find the first even number in a long list?"
18. **The Escape Artist:** Ask Gemini: "What is the difference between `break` and `continue` in Python loops?"
19. **The Real-World Modeler:** Ask Gemini: "How does a car engine use a 'break' logic if it gets too hot?"
20. **The Challenge:** Ask Gemini: "Give me a hard Python puzzle where I have to use two `break` commands in two different loops!"

---

### 🌟 Stretch Goal for "Flash" Students
Build a **"Treasure Room"** game! 
1. Use `while True:` to keep the player in the room.
2. Every turn, they can "Search."
3. **If** they find "Gold", they have a choice: "Leave" with the gold or "Keep Searching."
4. If they "Leave," use `break` to end the game and show their total loot!
 Riverside
